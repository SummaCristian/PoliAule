// API base URL. scripts/build-beta.sh overwrites this file at build time for
// the beta Cloudflare Pages project, so the value checked in here is the
// prod default.
export const API_BASE = 'https://api.poliaule.com';
